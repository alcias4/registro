<!DOCTYPE html>
<html lang="es">
    <head>
        <meta charset="UTF-8">
        <meta http-equiv="X-UA-Compatible" content="IE=edge">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>Document</title>
        <link rel="stylesheet" href="/css/registro.css">
    </head>
    <body>
        <main>
            <form action="/user/registro" method="POST" enctype="multipart/form-data">
                <a href="/">volver</a>
                <h1>registro</h1>
                <label>Nombre y apellido</label>
                <input type="text" name="nombre">
                <% if(locals && locals.validacion) {%> 
                    <% if(locals.validacion[0].param == "nombre"){ %> 
                        <label class="vali"><%= locals.validacion[0].msg %> </label>
                    <% } %> 
                <% } %>  
                <label>usuario</label>
                <input type="text" name="usuario">
                <% if(locals && locals.validacion) {%> 
                    <% if(locals.validacion[1].param == "usuario"){ %> 
                        <label class="vali"><%= locals.validacion[1].msg %> </label>
                    <% } %> 
                <% } %>  
                <label>Email</label>
                <input type="email" name="email">
                <% if(locals && locals.validacion) {%> 
                    <% if(locals.validacion[2].param == "email" ){ %> 
                        <label class="vali"><%= locals.validacion[2].msg %> </label>
                    <% } %> 
                <% } %>  
                <label>Contraseña</label>
                <input type="password" name="password">
                <% if(locals && locals.validacion) {%> 
                    <% if(locals.validacion[3].param == "password"){ %> 
                        <label class="vali"><%= locals.validacion[3].msg %> </label>
                    <% } %> 
                <% } %>  
                <label>Ingrese img de perfil</label>
                <input type="file" name="img" required>
                <button type="submit">Enviar</button>

            </form>
        </main>
    </body>
</html>